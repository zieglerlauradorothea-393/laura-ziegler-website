---
const values = [
  { text: 'Menschlichkeit & Fairness', color: 'coral' },
  { text: 'Herz & Leidenschaft', color: 'blue' },
  { text: 'Verbindlichkeit', color: 'yellow' },
  { text: 'Teamgeist', color: 'pink' },
];
---

<section class="py-16 md:py-20 lg:py-28 relative overflow-hidden" style="background-color: var(--color-lavender);" id="arbeitsweise">
  <!-- Decorative Bubbles (nur auf Desktop) -->
  <div class="hidden md:block absolute top-10 left-10 w-16 md:w-20 h-16 md:h-20 rounded-full opacity-50" style="background-color: var(--color-coral);" aria-hidden="true"></div>
  <div class="hidden md:block absolute top-32 right-20 w-10 md:w-14 h-10 md:h-14 rounded-full opacity-40" style="background-color: var(--color-mint);" aria-hidden="true"></div>
  <div class="hidden md:block absolute bottom-20 left-1/4 w-20 md:w-24 h-20 md:h-24 rounded-full opacity-30" style="background-color: var(--color-yellow);" aria-hidden="true"></div>
  <div class="hidden lg:block absolute bottom-40 right-10 w-16 h-16 rounded-full opacity-50" style="background-color: var(--color-pink);" aria-hidden="true"></div>
  
  <div class="container relative z-10">
    <div class="max-w-4xl mx-auto">
      <div class="mb-8 md:mb-12 text-center">
        <span class="tag mb-4 md:mb-6">Arbeitsweise</span>
        <h2 class="text-black mb-4 md:mb-6">
          So arbeite ich
        </h2>
        <p class="text-lg md:text-xl text-black/80 font-medium max-w-2xl mx-auto">
          Klar. Verlässlich. Mit Haltung. Ich bringe Ordnung rein – damit Inhalte wirken.
        </p>
      </div>

      <!-- Worauf ich Wert lege - als bunte Kacheln -->
      <div class="mb-8 md:mb-12">
        <h3 class="text-lg md:text-xl font-bold text-black mb-6 md:mb-8 text-center uppercase">Worauf ich Wert lege</h3>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 md:gap-4">
          {values.map((v) => (
            <div 
              class="rounded-2xl p-5 md:p-6 text-center hover:scale-105 transition-transform"
              style={`background-color: var(--color-${v.color});`}
            >
              <p class="text-black font-bold text-base md:text-lg">{v.text}</p>
            </div>
          ))}
        </div>
      </div>

      <div class="text-center">
        <a href="/kontakt" class="btn-primary">
          <span>Projekt anfragen</span>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </div>
  
  <!-- Sticker-Foto (nur auf großen Screens) -->
  <div class="hidden lg:block absolute bottom-8 right-8 xl:right-16">
    <div class="relative">
      <!-- Organischer Farbklecks -->
      <div 
        class="absolute -inset-3 scale-125"
        style="background-color: var(--color-mint); border-radius: 54% 46% 63% 37% / 48% 55% 45% 52%;"
        aria-hidden="true"
      ></div>
      <!-- Foto -->
      <img 
        src="/images/laura-arbeite.png" 
        alt="Laura Ziegler" 
        class="relative z-10 w-28 xl:w-36 h-28 xl:h-36 rounded-full object-cover object-top border-4 border-white shadow-lg"
      />
    </div>
  </div>
</section>
